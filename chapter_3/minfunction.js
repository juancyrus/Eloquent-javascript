//A function 'function min(a, b) returns the min between two values
function min(a , b) {
        if (a < b)  return a;
        else return b;
}

// we test our code using the following 
console.log(min(0, 5));
